.h1 Gyms

#gyms
  - @gyms.each do |g|
    .gym
      = link_to g.name, gym_path(g)
      %a.url{ :href => g.url, :target => "_blank" } [&#8599;]
      added
      - if g.created_within_last_week?
        %strong= "#{time_ago_in_words(g.created_at)} ago"
      - elsif g.created_at?
        %i= "#{time_ago_in_words(g.created_at)} ago" 
      - else
        %i on day one

%br

%div
  Wodder is currently posting wods from
  %strong= "#{@gyms.count} gyms"
  all over the world.


