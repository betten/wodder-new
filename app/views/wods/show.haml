%h1 Wod

= render :partial => 'shared/wod', :locals => { :wod => @wod }

%h2 Comments
- unless @wod.has_comments?
  %i no comments yet!

- @wod.comments.each do |comment|
  = comment

- form_for(:comment) do |c|
  %div= c.text_area :text

  %div= c.submit
